{
  "name": "@modelstat/adapters-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./version": "./src/version.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'tsconfig paths-only; nothing to build'",
    "test": "node --import tsx --test src/**/*.test.ts",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@modelstat/core": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
