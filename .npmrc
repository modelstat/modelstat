# Don't auto-install optional peer deps. pnpm's default since v9 is to
# auto-install them, which drags transitive deprecated packages into
# the install tree. Restore the "only install what's actually imported"
# semantics.
auto-install-peers=false
