# Local secret-scan hook. One-time setup:  pipx install pre-commit && pre-commit install
# After that, every `git commit` is scanned by gitleaks (same .gitleaks.toml as CI).
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
