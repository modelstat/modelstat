{
  "name": "@modelstat/daemon-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./pipeline": "./src/pipeline/index.ts",
    "./config": "./src/config/index.ts",
    "./http/clamp": "./src/http/clamp.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'tsconfig paths-only; nothing to build'",
    "test": "node --import tsx --test src/**/*.test.ts",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@modelstat/core": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
